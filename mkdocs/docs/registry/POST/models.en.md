> **WARNING**  
> Models can't be sent via POST requests anymore. Mosqlimate V2 now requires the models to be
> inserted via the Platform. It can be done in the [Model Add](https://mosqlimate.org/model/add) page after login.
